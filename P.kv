#importing kv dependencies
#:include main.kv

#importing python dependencies
import P from main
import Popup from main
    

<P>:
    id: 'P'
    title: 'sorry!'
    auto_dismiss=False
    MDFloatLayout:
        MDLabel:
            text: "Sorry! Right now we are not looking for data from your demographic"
            font_name: 'Poppins-Medium.ttf'
            md_bg_color: app.theme_cls.primary_dark
            size_hint: .2, .1
            pos_hint : {"x" : .3, "top" : .8}
        MDLabel: 
            text: "Thank you for visiting :)"
            font_name: 'Poppins-Regular.ttf'
            md_bg_color: app.theme_cls.primary_light
            size_hint: .2, .1
            pos_hint : {"x" : .3, "center" : .6}
